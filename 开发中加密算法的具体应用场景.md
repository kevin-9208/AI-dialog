# 开发中加密算法的具体应用场景

结合实际开发场景，从常见到进阶举例说明：

## 1. 用户密码存储

**绝对不能**用明文或简单哈希（MD5/SHA-256单次哈希）存密码，要用专门的密码哈希算法。

```javascript
// Node.js 用 bcrypt
const bcrypt = require('bcrypt');

// 注册时
const hash = await bcrypt.hash(password, 12); // 12是salt rounds
await db.users.insert({ email, passwordHash: hash });

// 登录时
const isValid = await bcrypt.compare(inputPassword, user.passwordHash);
```

如果是 Supabase，这块它的 Auth 服务已经内置处理了（用 bcrypt），一般不需要自己实现，除非你想自己管理用户表。

## 2. HTTPS/TLS —— 传输层加密

浏览器应用和前后端通信最基础的一层。开发时通常不需要手写 TLS，而是：
- 本地开发用 `mkcert` 生成自签名证书跑 HTTPS
- 生产环境用 Let's Encrypt / Cloudflare 自动签发证书
- Tauri 桌面应用如果要请求外部 API，同样依赖系统信任的证书链

## 3. JWT（JSON Web Token）—— 身份验证令牌

签名（不是加密！）用户身份信息，防止篡改。

```javascript
const jwt = require('jsonwebtoken');

// 签发（HMAC-SHA256，对称）
const token = jwt.sign({ userId: 123, role: 'admin' }, SECRET_KEY, { expiresIn: '1h' });

// 或用 RS256（非对称），适合微服务场景：
// 认证服务用私钥签名，其他服务用公钥验证，不需要共享密钥
const token = jwt.sign(payload, privateKey, { algorithm: 'RS256' });

// 验证
const decoded = jwt.verify(token, SECRET_KEY);
```

Supabase 的 RLS（Row Level Security）策略底层也是靠解析 JWT 里的 `auth.uid()` 来做权限判断的。

## 4. 数据库字段级加密（敏感数据）

比如身份证号、银行卡号这类字段，即使数据库被拖库也不能明文暴露。

```javascript
const crypto = require('crypto');

function encrypt(text, key) {
  const iv = crypto.randomBytes(12); // GCM推荐12字节IV
  const cipher = crypto.createCipheriv('aes-256-gcm', key, iv);
  let encrypted = cipher.update(text, 'utf8', 'hex');
  encrypted += cipher.final('hex');
  const authTag = cipher.getAuthTag();
  return { encrypted, iv: iv.toString('hex'), authTag: authTag.toString('hex') };
}
```

在 Supabase/Postgres 里也可以用 `pgcrypto` 扩展直接在数据库层加密：
```sql
SELECT pgp_sym_encrypt('sensitive data', 'encryption-key');
```

## 5. 本地文件/配置加密（桌面应用场景）

比如你做 Tauri 应用需要在本地存 API Key、用户凭证时，不能明文写进 JSON 配置文件。

Rust 端可以用 `ring` 或 `aes-gcm` crate：
```rust
use aes_gcm::{Aes256Gcm, Key, Nonce};
use aes_gcm::aead::{Aead, KeyInit};

let key = Key::<Aes256Gcm>::from_slice(key_bytes);
let cipher = Aes256Gcm::new(key);
let nonce = Nonce::from_slice(nonce_bytes);
let ciphertext = cipher.encrypt(nonce, plaintext).unwrap();
```

更推荐直接调用系统的 **Keychain / Credential Manager / Secret Service**（macOS/Windows/Linux），Tauri 有 `tauri-plugin-stronghold` 或社区的 keyring 插件可以用,避免自己造密钥管理的轮子。

## 6. 浏览器端加密（Client-side Encryption）

如果做端到端加密聊天应用（E2EE），加密要在用户设备完成，服务器只存密文。

```javascript
// 用 Web Crypto API（浏览器原生，不需要额外库）
async function encryptMessage(message, key) {
  const iv = crypto.getRandomValues(new Uint8Array(12));
  const encoded = new TextEncoder().encode(message);
  const ciphertext = await crypto.subtle.encrypt(
    { name: 'AES-GCM', iv },
    key,
    encoded
  );
  return { ciphertext, iv };
}
```

密钥交换可以用 ECDH：
```javascript
const keyPair = await crypto.subtle.generateKey(
  { name: 'ECDH', namedCurve: 'P-256' },
  true,
  ['deriveKey']
);
```

这种模式下，如果配合 Supabase Realtime 做聊天，服务器和数据库只经手密文，即使数据库泄露也看不到聊天内容。

## 7. API 请求签名（防篡改/防重放）

第三方开放平台（微信支付、AWS API）常见做法：用 HMAC 给请求签名。

```javascript
const crypto = require('crypto');

function signRequest(params, secretKey) {
  const sortedParams = Object.keys(params).sort()
    .map(k => `${k}=${params[k]}`).join('&');
  return crypto.createHmac('sha256', secretKey)
    .update(sortedParams)
    .digest('hex');
}
```

服务端收到请求后用同样方式重新计算签名比对，防止请求被篡改或重放。

## 8. 区块链/加密货币场景

如果涉及 Web3 开发：
- **私钥签名交易**：ECDSA（比特币）或 EdDSA（部分链）
- **地址生成**：公钥哈希（SHA-256 + RIPEMD-160）
- **Merkle Tree**：批量验证交易完整性

## 9. 一次性验证码/密码重置链接

用带时效性和签名的 token，防止链接被猜测或重放：
```javascript
const crypto = require('crypto');
const token = crypto.randomBytes(32).toString('hex'); // 高熵随机token
// 存储 token 的哈希值到数据库，而不是明文token本身
const tokenHash = crypto.createHash('sha256').update(token).digest('hex');
```

---

## 选型建议速查表

| 场景 | 推荐算法 |
|---|---|
| 密码存储 | bcrypt / Argon2 |
| 传输加密 | TLS 1.3 (AES-GCM / ChaCha20) |
| 身份令牌 | JWT (HS256 单体应用 / RS256 微服务) |
| 数据库敏感字段 | AES-256-GCM / pgcrypto |
| 本地凭证存储 | 系统 Keychain（优先）> AES-256-GCM |
| 端到端加密 | Web Crypto API (ECDH + AES-GCM) |
| API签名 | HMAC-SHA256 |
| 数字签名/区块链 | ECDSA / EdDSA |
