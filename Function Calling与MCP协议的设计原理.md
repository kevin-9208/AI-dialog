好的，我来用图文结合的方式深入介绍这两个重要概念。## 一、什么是 Function Calling？

Function Calling（函数调用）是一种让大语言模型能够**主动请求调用外部工具或函数**的机制。它解决了一个根本问题：模型本身是静态的文字预测系统，无法实时获取数据、执行计算或操作外部系统。

**工作原理：** 开发者在调用模型时，附带一份"工具说明书"（JSON Schema），告诉模型"你可以使用哪些工具、每个工具接受什么参数"。模型在理解用户意图后，如果判断需要调用某个工具，就会输出一段结构化的 JSON 意图——比如 `{"name": "search_web", "arguments": {"query": "今日天气"}}`。客户端代码收到这个意图后，真正去执行函数，把结果返回给模型，模型再生成最终的自然语言回复。

**关键设计理念：** 模型本身永远不执行代码，它只负责"决策"和"推理"，真正的执行权留在开发者手中。这保证了安全边界清晰，也让系统更容易调试。

---

接下来看 MCP 协议，它是 Function Calling 之上的一个更大的标准化层：## 二、什么是 MCP（Model Context Protocol）？

MCP 是由 Anthropic 于 2024 年提出的**开放标准协议**，全称 Model Context Protocol。你可以把它理解为"AI 世界的 USB-C 接口"——在此之前，每家 AI 厂商、每个工具都有自己的私有对接格式，导致同一个"天气查询"工具，要对接 Claude 写一套，对接 GPT 再写一套，极度碎片化。

**MCP 定义了三种核心能力类型：**

**Tools（工具）** 是有副作用的动作，比如发送邮件、写入文件、调用 API，是模型主动"做事"的手段。**Resources（资源）** 是只读的上下文数据，比如把一个代码仓库的文件内容、数据库记录暴露给模型参考。**Prompts（提示词模板）** 是预设的工作流程，比如"代码审查"这个 Prompt 会自动带入最佳实践的指令。

**协议层面**，MCP 使用 JSON-RPC 2.0 作为消息格式，传输上分两种：本地通过 stdio（标准输入输出）通信，适合在同一台机器上运行的 Server；远程通过 HTTP + SSE（Server-Sent Events），适合云端服务。MCP Client 启动时会主动向 Server 查询 `tools/list`，自动发现所有可用能力，无需硬编码。

---

## 三、为何要这样设计？——设计哲学解析

**Function Calling 的设计逻辑** 核心在于"关注点分离"。模型只负责理解意图和结构化输出，执行权完全交给客户端。这有几个深层原因：首先，让模型直接执行代码风险极高，难以审计；其次，将"决策"和"执行"分开，开发者可以在执行前加入权限校验、用户确认等安全层；最后，结构化的 JSON 意图输出比自由文本更可靠，不容易出现歧义。

**MCP 的设计逻辑** 则是从工程化规模的角度出发。Anthropic 意识到，当 AI Agent 需要对接几十上百个工具时，"每次集成都要重新写胶水代码"这种方式无法扩展。MCP 让工具提供方一次性写好 Server，任何兼容 MCP 的 AI 应用都能直接接入，真正实现工具的"即插即用"。这和 USB 统一接口标准的逻辑完全相同。

**两者的关系**：Function Calling 是底层机制，MCP 是在它之上建立的标准化生态。MCP Server 本质上是一个用统一协议包装的工具提供方，当模型通过 MCP Client 调用某个工具时，底层仍然走的是 Function Calling 的推理逻辑——模型输出调用意图，Client 转发给 Server 执行，结果再返回给模型。MCP 解决的是"怎么把工具标准化地组织和发现"，Function Calling 解决的是"模型怎么表达调用意图"。两者合力，构成了现代 AI Agent 能够真正动手做事的技术基础。
